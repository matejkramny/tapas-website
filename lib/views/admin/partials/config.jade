.modal-header
    h3 Site configuration
.modal-body
    form(name="configForm")
        .form-group
            .input-group
                .input-group-addon Name
                input.form-control(type="text", required, ng-model="config.name")
        .form-group
            .input-group
                .input-group-addon Address
                textarea.form-control(ng-model="config.address", rows=5)
        .form-group
            .input-group
                .input-group-addon Opening hours
                textarea.form-control(required, ng-model="config.openHours", rows=5)
        .form-group
            .input-group
                .input-group-addon Twitter URL
                input.form-control(type="text", ng-model="config.twitter")
        .form-group
            .input-group
                .input-group-addon Facebook URL
                input.form-control(type="text", ng-model="config.facebook")
        .form-group
            .input-group
                .input-group-addon Instagram URL
                input.form-control(type="text", ng-model="config.instagram")
        .form-group
            .btn-group.input-group
                .input-group-addon Promo
                .btn(ng-class="{'btn-success': config.promo, 'btn-danger': !config.promo}", ng-click="toggle('promo')"): i.fa.fa-fw(ng-class="{'fa-check': config.promo, 'fa-times': !config.promo}")
                span.btn.btn-info.btn-file(ngf-select ngf-capture="'camera'" accept="image/*"  ngf-change="upload($files)") Click here to upload
        progressbar(max='100', value='upload.progress', ng-if="upload.progress > 0")
            span(style='color:white; white-space:nowrap;') {{ upload.progress }}% Uploaded
        input(type="hidden", name="hidden", ng-value="config.promo")
.modal-footer
    a.btn.btn-success(ng-click="ok()", ng-disabled="configForm.$invalid")
        i.fa.fa-check
        |  Submit